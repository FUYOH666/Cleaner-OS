# Пример конфигурации для секретов
# Скопируйте в .env и заполните реальными значениями

# SSH ключи для проверки (опционально)
# SSH_KEYS_PATH=~/.ssh

# Пути для исключения из сканирования (дополнительно к config.yaml)
# EXCLUDE_PATHS=

# Уровень логирования (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

